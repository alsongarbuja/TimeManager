@using TimeManager.Backend.ViewModels.PartialViews
@using TimeManager.Backend.ViewModels
@model IEnumerable<EmployeeTypeViewModel>

@{
    Layout = "_ListWrapper";
    ViewData["Title"] = "Employee Type";

    List<string> Headers = ["Name", "Description"];
    ViewData["Headers"] = Headers;
    ViewData["ModuleTitle"] = "Employee Type";
    ViewData["ModuleDesc"] = "Manage the employee types in the system";
    ViewData["Controller"] = "EmployeeType";
    ViewData["DataCount"] = Model.Count();
}

@foreach (var d in Model)
{
    <tr class="table-row">
        <sn-cell counter-name="EmployeeType" start="0" />
        <td class="table-cell">@d.Name</td>
        <td class="table-cell table-desc">@d.Description</td>
        @await Html.PartialAsync("ActionTd", new ActionTdViewModel
        {
            Controller = "EmployeeType",
            Id = d.Id,
        })
    </tr>
}