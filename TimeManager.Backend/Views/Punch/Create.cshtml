@model TimeManager.Backend.ViewModels.PunchViewModel

@{
    Layout = "_FormWrapper";
    ViewData["Title"] = "Punch - Create";
    ViewData["ModuleTitle"] = "Create Punch";
    ViewData["ModuleSubtitle"] = "Create punch data";
    ViewData["Controller"] = "Punch";
}

<form asp-controller="Punch" asp-action="Create" method="post">
    @Html.AntiForgeryToken()

    <custom-select asp-for="EmployeeId" asp-items="Model.Employees" searchable />
    <custom-input asp-for="ClockInTime" max="@DateTime.Now.ToString("yyyy-MM-ddTHH:mm")" />
    <custom-input asp-for="ClockOutTime" max="@DateTime.Now.ToString("yyyy-MM-ddTHH:mm")" />

    <button type="submit" class="btn btn-primary">Submit</button>
</form>
