<aside class="fixed top-0 left-0 z-40 w-64 h-screen bg-black text-white flex flex-col justify-between p-6 transition-transform -translate-x-full lg:translate-x-0 border-r border-neutral-900">
    <div>
        <div class="flex items-center gap-3 pb-6 mb-8 border-b border-neutral-800">
            <div class="w-8 h-8 bg-white text-black flex items-center justify-center rounded font-black text-lg">Ω</div>
            <span class="text-sm font-bold tracking-widest uppercase">Time Clock</span>
        </div>
        <nav class="space-y-6">
            <div>
                <ul class="space-y-1">
                    <li>
                        <SidebarNav 
                            Text="Dashboard"
                            Url="/app/dashboard"
                            IconClass="ri-layout-grid-line"
                            />
                    </li>
                </ul>
            </div>
            @* @if (request.role.name != "Manager") *@
            <div>
                <span class="px-3 text-xxs font-semibold uppercase tracking-wider text-neutral-500 block mb-2">Time Track</span>
                <ul class="space-y-1">
                    <li>
                        <SidebarNav Text="Clock in / out"
                                    Url="/app/clock-in-out"
                                    IconClass="ri-time-line" />
                    </li>
                    <li>
                        <SidebarNav Text="Time off requests"
                                    Url="/app/timeoff-request"
                                    IconClass="ri-list-check" />
                    </li>
                </ul>
            </div>
            @* {% endif %} *@
            @* {% if request.role.name == "Manager" %} *@
            <div>
                <span class="px-3 text-xxs font-semibold uppercase tracking-wider text-neutral-500 block mb-2">Management</span>
                <ul class="space-y-1">
                    <li>
                        <SidebarNav Text="Punch Report"
                                    Url="/app/punch-report"
                                    IconClass="ri-file-chart-line" />
                    </li>
                    <li>
                        <SidebarNav Text="Schedule"
                                    Url="/app/scheduler"
                                    IconClass="ri-calendar-line" />
                    </li>
                </ul>
            </div>
            @* {% endif %} *@
        </nav>
    </div>
</aside>

@code {

}
