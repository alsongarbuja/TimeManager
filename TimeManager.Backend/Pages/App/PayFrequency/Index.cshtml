@page
@model TimeManager.Backend.Pages.App.PayFrequency.IndexModel
@{
    Layout = "_ListWrapper";
    ViewData["Title"] = "PayFrequency";

    List<string> Headers = ["Name", "Description"];
    ViewData["Headers"] = Headers;
    ViewData["ModuleTitle"] = "PayFrequency";
    ViewData["ModuleDesc"] = "Manage the pay frequencies in the system";
    ViewData["CreateUrl"] = "payfrequency/add";
}

@if (Model.PayFrequencies.Count() <= 0)
{
    <tr>
        <td colspan="@Headers.Count"
            class="table-empty-cell">
            No data available
        </td>
    </tr>
}
else
{
    foreach (var d in Model.PayFrequencies)
    {
        <tr class="table-row">
            <td class="table-cell">@d.Name</td>
            <td class="table-cell table-desc">@d.Description</td>
        </tr>
    }
}